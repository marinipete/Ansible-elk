---
- hosts: all
  remote_user: qaadm
  become: yes
  become_user: root
  tasks:
    - name: Validando se o binário existe no servidor remoto
      include: tasks/bin_version.yml app_name_extracao={{ item }}
      with_items: "{{ app_name.split('|') }}"
